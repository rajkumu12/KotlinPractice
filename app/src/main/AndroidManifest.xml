<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kptlin.kotlinpractice">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CustomToast" />
        <activity android:name=".com.intent.example.ExplicitIntentEx" />
        <activity android:name=".com.intent.example.SecondActivity" />
        <activity android:name=".com.intent.example.ImplicitIntentEx" />
        <activity android:name=".ListViewExample.ListViewActivity" />
        <activity android:name=".AlertDialog.AltDialogExample"></activity>
    </application>

</manifest>